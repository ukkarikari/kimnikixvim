{
  config = {
    colorschemes.base16 = {
      enable = true;
      colorscheme = "gruvbox-material-dark-medium";
    };

    # clipboard.providers.wl-copy.enable = true;

    opts = {
      # Line numbers
      number = true;
      relativenumber = true;

      # Tabs
      smartindent = true;
      shiftwidth = 2;

      # Text wrap
      wrap = true;
    };
  };
}
